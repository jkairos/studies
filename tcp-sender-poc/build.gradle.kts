/*
 * This file was generated by the Gradle 'init' task.
 */

import com.google.common.collect.ImmutableMap
import org.springframework.boot.gradle.tasks.bundling.BootJar

plugins {
    id("com.palantir.docker") version "0.35.0"
    id("com.palantir.docker-run") version "0.35.0"
    id("java")
    id("org.springframework.boot") version "2.7.11"
    id("org.openapi.generator") version "6.6.0"
    id("org.springdoc.openapi-gradle-plugin") version "1.6.0"
    id ("io.spring.dependency-management") version "1.0.15.RELEASE"
}

sourceSets["main"].java.srcDir(file("$rootDir/build/generated"))

repositories {
    mavenLocal()
    mavenCentral()
    maven { url = uri("https://repo.spring.io/milestone") }
    maven { url = uri("https://repo.spring.io/snapshot") }
}

configurations.all {
    resolutionStrategy{
        exclude(module="slf4j-simple")
    }
}

dependencies {
    implementation("io.rest-assured:rest-assured:3.3.0")
    implementation("org.springframework.boot:spring-boot-starter-web")
    implementation("org.springframework.boot:spring-boot-starter-actuator")
    implementation("org.openapitools:openapi-generator:7.0.1")
    implementation("org.openapitools:openapi-generator-gradle-plugin:6.6.0")
    implementation("org.openapitools:jackson-databind-nullable:0.2.6")
    implementation("javax.validation:validation-api:2.0.1.Final")
    implementation("io.swagger.core.v3:swagger-annotations:2.2.16")
    implementation("io.swagger.core.v3:swagger-models:2.2.16")
    implementation("org.projectlombok:lombok:1.18.30")
    implementation("io.springfox:springfox-boot-starter:3.0.0")
    implementation("org.springframework.boot:spring-boot-starter-test")
    annotationProcessor("org.projectlombok:lombok:1.18.30")
}

group = "net.vox.messaging"
version = "1.0.0-SNAPSHOT"
description = "tcp-sender-poc"
val mainClassName = "net.cox.messaging.TcpSenderPoCApplication"

//gradle docker plugin configuration
apply(plugin = "com.palantir.docker")
apply(plugin = "java-library")
apply(plugin = "org.openapi.generator")

tasks.withType<BootJar> {
    manifest {
        attributes("Start-Class" to "$mainClassName")
    }
    archiveClassifier.set("")
}

springBoot {
    mainClass.set("$mainClassName")
}


docker {
    name = "tcp-sender-poc:".plus(version)
    uri("tcp-sender-poc:".plus(version))
    tag("name", "tcp-sender-poco")
    buildArgs(ImmutableMap.of("name", "tcp-sender-poc"))
    copySpec.from("build").into("build")
    pull(true)
    setDockerfile(file("Dockerfile"))
}

dockerRun {
    name = "tcp-sender-poc"
    image = "tcp-sender-poc:".plus(version)
    ports("8080:8080")
}

openApi {
    apiDocsUrl.set("http://localhost:5000/v3/api-docs")
    outputDir.set(file("$rootDir/build/docs"))
    outputFileName.set("swagger.yaml")
}

openApiGenerate {
    generatorName.set("spring")
    inputSpec.set("$rootDir/src/main/resources/static/tcp-sender-poc.yaml")
    outputDir.set("$rootDir/build/generated")
    apiPackage.set("net.cox.api")
    invokerPackage.set("net.cox.invoker")
    modelPackage.set("net.cox.model")
}

tasks {
    val openApiGenerate by getting

    val compileJava by getting {
        dependsOn(openApiGenerate)
    }
}

tasks.dockerPrepare.configure {
    dependsOn(tasks.compileJava,
        tasks.bootJar,
        tasks.compileTestJava,
        tasks.dockerfileZip,
        tasks.processResources,
        tasks.processTestResources,
        tasks.test)
}

tasks.openApiGenerate.configure {
    dependsOn(tasks.processResources)
}

